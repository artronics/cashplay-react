import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  width: 480px;
  height: 480px;
  border: 1px solid gray;
`;
const PickPickerBox = (props) => (
  <Wrapper>
    <div>picture</div>
  </Wrapper>
);

export default PickPickerBox;
